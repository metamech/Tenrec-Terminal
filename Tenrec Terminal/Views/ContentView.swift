import SwiftUI

struct ContentView: View {
    var body: some View {
        Text("Tenrec Terminal")
            .font(.largeTitle)
            .frame(maxWidth: .infinity, maxHeight: .infinity)
    }
}

#Preview {
    ContentView()
}
